{
  "name": "scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VeriCell operational scripts: service-fee pool setup and sweeping.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint ."
  },
  "dependencies": {
    "chain": "workspace:*",
    "commander": "^15.0.0",
    "core": "workspace:*"
  }
}
