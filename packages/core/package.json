{
  "name": "core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VeriCell pure logic: hashing, Merkle, manifest.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
